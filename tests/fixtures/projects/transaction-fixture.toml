schema_version = 1

[project]
id = "transaction-fixture"
name = "Orchestra Transaction Fixture"
enabled = false
policy = "default"

[paths]
repo = "/opt/orchestra/workspaces/.fixtures/transaction-fixture"
data = "/opt/orchestra/project-data/.fixtures/transaction-fixture"

[git]
default_branch = "main"
allow_push = false
require_clean = true

[execution]
writer_concurrency = 1
max_parallel_tasks = 1

[review]
required = true
